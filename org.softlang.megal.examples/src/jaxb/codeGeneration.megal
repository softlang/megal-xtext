/*
 * Arguably, the typical Object/XML mapping technology comes with a code generation
 * component to derive OO types from XML types. Conceptually, though, this component is optional.
 */
model CodeGeneration 

import Prelude
import Types

OxMapper.Generator : Function[XmlSchemas -> MappableOo]
OxMapper.Generator(xmlTypes) |-> ooTypes